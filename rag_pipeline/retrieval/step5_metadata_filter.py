from __future__ import annotations


def apply_metadata_filter(chunks: list[dict], _filters: dict | None = None) -> list[dict]:
    """Reserved for Lab 2; Lab 1 leaves the vector results unchanged."""
    return chunks

